---
title: 계획(PLAN) + 구현
description: 무엇을 만들지 계획하고, 클로드코드의 Plan 모드로 구현을 준비해요.
---

import { Callout } from 'nextra/components'
import { Lightbulb, ClipboardList, Search, GitBranch, Layers } from 'lucide-react'

# 03. 계획(PLAN) + 구현

![Claude Code Plan Mode](/images/claude-code-plan-mode.webp)

## 원래는 이런 것들을 해야 해요

본격적인 개발 전에는 보통 이런 과정을 거쳐요:

- <Lightbulb size={16} color="#f59e0b" style={{display:'inline', verticalAlign:'middle', marginRight:'6px'}} />**브레인스토밍** — 어떤 제품을 만들지, 핵심 가치는 무엇인지
- <ClipboardList size={16} color="#818cf8" style={{display:'inline', verticalAlign:'middle', marginRight:'6px'}} />**기능 정의** — 어떤 기능을 넣을지, 우선순위는 어떻게 할지
- <Search size={16} color="#38bdf8" style={{display:'inline', verticalAlign:'middle', marginRight:'6px'}} />**경쟁 제품 리서치** — 비슷한 제품들의 특징, 강점, 차별화 포인트 분석
- <GitBranch size={16} color="#34d399" style={{display:'inline', verticalAlign:'middle', marginRight:'6px'}} />**사용자 흐름 설계** — 사용자가 어떤 경로로 움직이는지
- <Layers size={16} color="#fb923c" style={{display:'inline', verticalAlign:'middle', marginRight:'6px'}} />**기술 스택 결정** — 어떤 언어, 프레임워크, 인프라를 쓸지

<Callout type="info">
  실제 서비스를 만들 때는 이 과정이 매우 중요해요.\
  하지만 이번 실습은 **심플한 포트폴리오 사이트**를 만드는 것이 목표라 이 과정은 건너뛸게요.
</Callout>

---

## 대신 클로드코드의 Plan 모드를 써볼게요

클로드코드에는 크게 두 가지 모드가 있어요.

<div className="not-prose grid grid-cols-1 sm:grid-cols-2 gap-px bg-border my-6 border border-border text-sm">
  <div className="bg-background px-5 py-4">
    <div className="font-semibold text-foreground mb-1">⚡ 기본 모드</div>
    <div className="text-muted-foreground mb-2">말하면 바로 실행해요</div>
    <div className="text-muted-foreground">요청하는 순간 파일을 수정하고 명령어를 실행해요. 빠르게 작업할 수 있지만, 생각지 못한 파일까지 바뀔 수 있어요.</div>
  </div>
  <div className="bg-background px-5 py-4">
    <div className="font-semibold text-foreground mb-1">📋 Plan 모드</div>
    <div className="text-muted-foreground mb-2">계획을 먼저 세우고 확인받아요</div>
    <div className="text-muted-foreground">코드를 읽고 분석하기만 해요. 아무것도 수정하지 않고 "어떤 파일을 어떤 순서로 바꿀지" 계획서를 먼저 작성해줘요. 내가 OK한 뒤에만 실제 작업을 시작해요.</div>
  </div>
</div>

처음 프로젝트를 시작하거나 큰 변경을 앞두고 있다면 Plan 모드로 먼저 계획을 잡는 게 훨씬 안전해요.\
**클로드코드의 [`/plan` 모드](/reference/commands/plan)** 로 기획 단계 없이도 비슷한 효과를 낼 수 있어요.

### 이럴 때 쓰면 좋아요

처음 프로젝트를 시작할 때만 쓰는 게 아니에요. 개발하다가 막히는 순간마다 꺼내 쓸 수 있는 도구예요.

<div className="not-prose grid grid-cols-1 sm:grid-cols-2 gap-px bg-border my-6 border border-border text-sm">
  <div className="bg-background px-5 py-4">
    <div className="font-semibold text-foreground mb-1">새 프로젝트 시작</div>
    <div className="text-muted-foreground">처음부터 구조를 잡고 시작하면 나중에 대규모 수정이 줄어요.</div>
  </div>
  <div className="bg-background px-5 py-4">
    <div className="font-semibold text-foreground mb-1">기능을 크게 추가할 때</div>
    <div className="text-muted-foreground">영향받는 파일이 많을수록 계획 없이 시작하면 꼬여요.</div>
  </div>
  <div className="bg-background px-5 py-4">
    <div className="font-semibold text-foreground mb-1">남이 만든 프로젝트를 이어받을 때</div>
    <div className="text-muted-foreground">처음 보는 코드도 Claude가 먼저 파악하고 설명해줘서 실수를 줄여요.</div>
  </div>
  <div className="bg-background px-5 py-4">
    <div className="font-semibold text-foreground mb-1">버그 원인이 불명확할 때</div>
    <div className="text-muted-foreground">수정 전에 원인을 찾고 범위를 파악할 수 있어요.</div>
  </div>
  <div className="bg-background px-5 py-4">
    <div className="font-semibold text-foreground mb-1">코드를 정리·개선할 때</div>
    <div className="text-muted-foreground">기능은 그대로 두고 구조를 깔끔하게 다듬을 때, 어떤 파일을 어떤 순서로 바꿔야 할지 계획을 세워줘요.</div>
  </div>
  <div className="bg-background px-5 py-4">
    <div className="font-semibold text-foreground mb-1">무언가 망가질까 봐 불안할 때</div>
    <div className="text-muted-foreground">Plan 모드에선 아무것도 바꾸지 않으니 안심하고 탐색할 수 있어요.</div>
  </div>
</div>

<Callout type="tip">
  <span className="inline-block text-xs font-semibold tracking-widest uppercase px-2 py-0.5 mb-2 bg-emerald-500/15 text-emerald-600 dark:text-emerald-400 border border-emerald-500/30">Best Practice</span>
  **"뭔가 큰 걸 바꾸려는데 어디서부터 시작해야 할지 모르겠다"** 싶을 때 습관적으로 `/plan`을 먼저 입력해보세요.\
  Claude가 길을 먼저 열어줄 거예요.
</Callout>

---

## Plan 모드 전에 먼저 모델을 설정해요

클로드코드는 작업의 복잡도에 따라 사용하는 AI 모델을 바꿀 수 있어요.\
간단한 수정엔 빠르고 저렴한 모델을, 복잡한 계획이 필요할 땐 더 똑똑한 모델을 선택하는 식이에요.\
작업에 맞는 모델을 쓰면 속도와 비용을 모두 잡을 수 있어요.

선택할 수 있는 모델은 세 가지예요.

| 모델 | 속도 | 비용 | 특징 |
|------|------|------|------|
| **Haiku** | 가장 빠름 | 가장 저렴 | 간단한 수정, 짧은 질문에 적합해요 |
| **Sonnet** | 빠름 | 중간 | 일반적인 코딩 작업에 가장 균형 잡힌 선택이에요. 기본값이에요 |
| **Opus** | 느림 | 가장 비쌈 | 가장 똑똑해요. 복잡한 구조 분석이나 계획 수립에 강해요 |

**1단계 — 클로드코드 터미널에서 [`/model`](/reference/commands/model)을 입력해요**

<SlashBlock>
```
/model
```
</SlashBlock>

**2단계 — 메뉴에서 `Opus Plan Mode`를 선택해요**

방향키로 4번 항목으로 이동한 뒤 Enter를 눌러요.

<ClaudeBlock>
```
  1. Default (recommended)  Opus 4.7 with 1M context
  2. Sonnet                 Sonnet 4.6
  3. Haiku                  Haiku 4.5
❯ 4. Opus Plan Mode ✔       Use Opus in plan mode, Sonnet otherwise
```
</ClaudeBlock>

4번 항목인 **Opus Plan Mode**는 조금 특별해요.\
계획을 세울 때는 가장 똑똑한 Opus가 투입되고, 실제 코드를 짤 때는 빠른 Sonnet으로 자동으로 바뀌어요.\
"머리 쓸 때는 Opus, 손발은 Sonnet"인 셈이에요.

<Callout type="info">
  <span className="inline-block text-xs font-semibold tracking-widest uppercase px-2 py-0.5 mb-2 bg-emerald-500/15 text-emerald-600 dark:text-emerald-400 border border-emerald-500/30">Best Practice</span>
  기본 모델(Sonnet)로도 Plan 모드를 쓸 수 있지만,\
  **Opus Plan Mode**로 설정하면 계획 단계에서만 Opus가 투입돼 더 깊이 분석해줘요.\
  코드 작성 단계는 다시 Sonnet으로 자동 전환되니 비용도 효율적이에요.
</Callout>

---

## Plan 모드로 전환

모델 설정이 끝났으면 이제 Plan 모드로 전환해볼게요.

`/plan`을 입력하거나, **Shift + Tab**을 두 번 눌러도 돼요.

<SlashBlock>
```
/plan
```
</SlashBlock>

이 명령어를 입력하는 순간 Claude가 Plan 모드로 전환돼요.\
이 상태에서 원하는 걸 말하면 코드를 바로 짜는 대신, 어떻게 만들지 계획부터 세워줘요.\
파일을 건드리지 않고 분석과 계획만 하는 안전한 상태예요.

---

### 실제로 Plan 모드에서 말을 걸면 이런 식으로 진행돼요

내가 명확하게 말하지 않은 부분이 있으면 Claude가 인터뷰하듯 하나씩 물어봐요.\
"어떤 섹션이 필요해요?", "다크 모드가 필요해요?" 같은 식으로요.\
대답을 충분히 모은 뒤에 어떤 파일을 어떤 순서로 만들지 단계별 계획서를 작성해줘요.\
계획을 확인받은 후에만 실제 코드 작성을 시작하기 때문에 의도치 않은 수정이 일어나지 않아요.

<VibeBlock>
```
나만의 포트폴리오 웹사이트를 만들고 싶어.
```
</VibeBlock>

<ClaudeBlock>
```
⏺ 어떤 기술 스택을 사용하실 건가요?
  Next.js + TypeScript + Tailwind CSS + shadcn/ui 조합으로 진행할까요?
```
</ClaudeBlock>

<VibeBlock>
```
응, 그렇게 해줘.
```
</VibeBlock>

<ClaudeBlock>
```
⏺ 어떤 섹션이 필요하신가요?
  (예: 자기소개, 스킬, 프로젝트, 경력/학력, 연락처 등)
```
</ClaudeBlock>

<VibeBlock>
```
자기소개, 스킬, 프로젝트, 경력/학력, 연락처 다 넣어줘.
```
</VibeBlock>

<ClaudeBlock>
```
⏺ 프로젝트 카드에 GitHub 링크나 데모 링크도 들어가야 하나요?
```
</ClaudeBlock>

<VibeBlock>
```
응, 둘 다 넣고 싶어.
```
</VibeBlock>

<ClaudeBlock>
```
⏺ 프로젝트, 스킬, 경력 데이터는 어떻게 관리하실 건가요?
  - 코드 파일에 직접 작성 (간단하고 빠름)
  - Supabase DB에서 불러오기 (나중에 관리자 페이지 추가 가능)
```
</ClaudeBlock>

<VibeBlock>
```
일단 코드에 직접 쓸게.
```
</VibeBlock>

<ClaudeBlock>
```
⏺ 다크 모드와 모바일 대응이 필요하신가요?
```
</ClaudeBlock>

<VibeBlock>
```
둘 다 필요해.
```
</VibeBlock>

아래가 실제로 Claude가 만들어주는 계획 출력이에요:

<ClaudeBlock>
```
⏺ 알겠어요. 구현 계획을 정리했어요:

  ## 포트폴리오 사이트 계획

  ### 만들 파일
  - app/page.tsx              — 메인 페이지 (모든 섹션 조합)
  - app/layout.tsx            — 공통 레이아웃, 상단 네비게이션, 다크 모드 설정
  - components/Hero.tsx       — 이름, 직무, 한 줄 소개, 연락하기 버튼
  - components/Skills.tsx     — 카테고리별 기술 스택 목록
  - components/Projects.tsx   — 프로젝트 카드 그리드 (GitHub/데모 링크 포함)
  - components/Career.tsx     — 경력/학력 타임라인
  - components/Contact.tsx    — 이메일, GitHub, LinkedIn 연락처
  - data/projects.ts          — 프로젝트 데이터 (여기서 직접 수정)
  - data/skills.ts            — 스킬 데이터 (여기서 직접 수정)
  - data/career.ts            — 경력/학력 데이터 (여기서 직접 수정)

  ### 작업 순서
  1. Next.js 프로젝트 생성 + Tailwind CSS, shadcn/ui 설치
  2. 공통 레이아웃 + 상단 고정 네비게이션 구현
  3. Hero 섹션 구현
  4. 스킬 섹션 구현
  5. 프로젝트 카드 그리드 구현
  6. 경력/학력 타임라인 구현
  7. 연락처 섹션 구현
  8. 다크 모드 + 모바일 반응형 적용
  9. Vercel 배포 설정

  이 계획대로 진행할까요?
```
</ClaudeBlock>

<VibeBlock>
```
응, 계획대로 진행해줘.
```
</VibeBlock>

<ClaudeBlock>
```
⏺ 알겠어요. Plan 모드를 종료하고 코드 작성을 시작할게요.

  ✦ Next.js 프로젝트 생성 중...
  ✦ app/layout.tsx 생성 중...
  ✦ components/Hero.tsx 생성 중...
```
</ClaudeBlock>

실수 없이 큰 그림을 먼저 잡을 수 있는 안전한 첫 단계예요.

이렇게 하면 계획은 Opus가, 코드 작성은 Sonnet이 담당하는 최적의 조합이 만들어져요.

| 단계 | 사용 모델 | 역할 |
|------|-----------|------|
| `/plan` 모드 | **Opus** | 코드베이스 분석, 구조 파악, 계획 수립 |
| 코드 작성 | **Sonnet** | 빠르고 안정적인 구현 |

자세한 내용은 [`/model` 레퍼런스](/reference/commands/model)와 [`/plan` 레퍼런스](/reference/commands/plan)를 참고하세요.

---

## 이제 클로드코드에게 말을 걸어볼게요

Plan 모드에서 자연어로 원하는 것을 설명하면 돼요.\
완벽하게 정리된 기획서가 없어도 괜찮아요 — 아이디어를 편하게 말하면 Claude가 구체화해줘요.

이번 실습에서 사용할 기술 스택은 아래와 같아요. Claude에게 말을 걸 때 이 스택을 함께 알려주면 더 정확하게 세팅해줘요.

<Callout type="tip">
  <span className="inline-block text-xs font-semibold tracking-widest uppercase px-2 py-0.5 mb-3 bg-emerald-500/15 text-emerald-600 dark:text-emerald-400 border border-emerald-500/30">이번 실습 기술 스택</span>
  
  | 기술 | 한 줄 설명 |
  |------|-----------|
  | **Next.js** | 웹사이트를 만드는 React 기반 프레임워크예요. 폴더 구조만으로 페이지가 자동으로 만들어져요. |
  | **TypeScript** | JavaScript에 타입을 추가한 언어예요. 코드 실수를 미리 잡아줘서 더 안전한 개발이 가능해요. |
  | **App Router** | Next.js의 최신 라우팅 방식이에요. `app/` 폴더 안에 파일을 만들면 자동으로 URL이 생성돼요. |
  | **Supabase** | 데이터베이스 + 로그인 기능을 쉽게 쓸 수 있는 서비스예요. 백엔드 없이도 데이터를 저장할 수 있어요. |
  | **Vercel** | Next.js를 만든 회사가 운영하는 배포 플랫폼이에요. GitHub에 올리면 자동으로 배포돼요. |
  | **Tailwind CSS** | 클래스 이름으로 디자인하는 CSS 도구예요. `text-lg`, `bg-blue-500` 같은 이름으로 스타일을 바로 적용해요. |
  | **shadcn/ui** | 버튼, 카드, 모달 같은 UI 컴포넌트 모음이에요. 복사해서 바로 쓸 수 있고 커스터마이징도 쉬워요. |
</Callout>

아래는 포트폴리오 사이트를 시작할 때 쓸 수 있는 예시들이에요:

<VibeBlock>
```
나만의 포트폴리오가 있는 웹사이트를 만들고 싶어.
```
</VibeBlock>

<VibeBlock>
```
개발자 포트폴리오 사이트 만들어줘.
자기소개, 프로젝트 목록, 연락처 섹션이 있었으면 좋겠어.
```
</VibeBlock>

<VibeBlock>
```
심플하고 깔끔한 포트폴리오 웹사이트를 만들고 싶어.
Next.js 쓰고 싶고, 다크 모드도 지원하면 좋겠어.
프로젝트 카드들이 그리드로 보이는 레이아웃이면 좋겠어.
```
</VibeBlock>

<VibeBlock>
```
포트폴리오 사이트 만들어줘.
히어로 섹션에 내 이름이랑 한 줄 소개가 나오고,
아래에 내가 만든 프로젝트들이 카드 형태로 보이는 구성이면 돼.
GitHub 링크도 넣을 수 있으면 좋겠어.
```
</VibeBlock>

기술 스택을 명시하면 Claude가 세팅부터 맞춰줘요:

<VibeBlock>
```
Next.js + TypeScript + Tailwind CSS + shadcn/ui로
포트폴리오 사이트 만들어줘.
자기소개, 프로젝트 목록, 연락처 섹션이 있었으면 좋겠어.
```
</VibeBlock>

<VibeBlock>
```
Next.js App Router, TypeScript, Tailwind CSS, shadcn/ui 스택으로
심플한 개발자 포트폴리오 만들어줘.
데이터는 Supabase에 저장하고 Vercel로 배포할 거야.
프로젝트 카드들이 그리드로 보이고 다크 모드도 지원하면 좋겠어.
```
</VibeBlock>

이번 실습에서는 아래 프롬프트를 그대로 사용할 거예요:

<Callout type="tip">
  <span className="inline-block text-xs font-semibold tracking-widest uppercase px-2 py-0.5 mb-2 bg-blue-500/15 text-blue-600 dark:text-blue-400 border border-blue-500/30">실습</span>
  다음 프롬프트를 Plan 모드에서 그대로 입력해보세요.\
  Claude가 질문을 던지면 그때 답하면서 구체화하는 것이 자연스러운 흐름이에요.
</Callout>

<VibeBlock>
```
나만의 포트폴리오가 있는 웹사이트를 만들고 싶어.
Next.js App Router, TypeScript, Tailwind CSS, shadcn/ui 기술스택을 사용할거고
DB연동은 Supabase, 배포는 Vercel을 이용할 거야.
```
</VibeBlock>

---

## 이런 식으로 대화가 흘러가요

Plan 모드를 시작하면 Claude가 바로 코드를 짜지 않아요.\
먼저 뭘 만들고 싶은지 물어보고, 충분히 파악한 뒤에 계획을 세워요.\
마치 개발자 친구와 아이디어를 정리하는 대화 같은 느낌이에요.

---

### 1단계 — 내가 무엇을 만들고 싶은지 말해요

<VibeBlock>
```
> 나만의 포트폴리오가 있는 웹사이트를 만들고 싶어.
  Next.js App Router, TypeScript, Tailwind CSS, shadcn/ui 기술스택을 사용할거고
  DB연동은 Supabase, 배포는 Vercel을 이용할 거야.
```
</VibeBlock>

---

### 2단계 — Claude가 구체적으로 물어봐요

Claude가 모호한 부분을 스스로 가정하지 않고 먼저 확인해요.

<ClaudeBlock>
```
⏺ 포트폴리오 사이트 계획을 시작하기 전에 몇 가지 여쭤볼게요.

  1. 어떤 페이지나 섹션이 필요하신가요?
     (예: 자기소개 히어로, 프로젝트 목록, 기술 스택, 경력, 연락처 등)

  2. 프로젝트 데이터는 어떻게 관리하실 건가요?
     - 코드 파일에 직접 작성 (간단하고 빠름)
     - Supabase DB에서 불러오기 (나중에 관리자 페이지 추가 가능)

  3. 다크 모드가 필요하신가요?

  4. 모바일에서도 잘 보여야 하나요?
```
</ClaudeBlock>

---

### 3단계 — 내가 답해요

`>` 뒤에 타이핑하고 Enter를 누르면 돼요.\
완벽하게 답하지 않아도 괜찮아요. 생각나는 대로 말하면 Claude가 맞춰줘요.

<VibeBlock>
```
> 섹션은 자기소개, 프로젝트 목록, 연락처면 충분해.
  프로젝트 데이터는 일단 코드에 직접 쓸게.
  다크 모드랑 모바일 대응 둘 다 필요해.
```
</VibeBlock>

모르거나 정해지지 않은 건 그냥 말해도 돼요:

<VibeBlock>
```
> DB 연동은 나중에 추가하고 싶은데 일단 없이 시작해도 될까?
```
</VibeBlock>

<ClaudeBlock>
```
⏺ 네, 전혀 문제없어요. 지금은 코드에 데이터를 직접 작성하고,
  나중에 Supabase 연동이 필요하면 그때 추가하는 방식으로 할게요.
```
</ClaudeBlock>

---

### 4단계 — Claude가 계획을 만들어줘요

대화가 끝나면 Claude가 어떤 파일을 만들고 어떤 순서로 작업할지 정리해줘요.

<ClaudeBlock>
```
⏺ 알겠어요. 구현 계획을 정리했어요:

  ## 포트폴리오 사이트 계획

  ### 만들 화면
  - 메인 페이지 — Hero(자기소개), 프로젝트 목록, 연락처를 한 페이지에 배치

  ### 만들 파일
  - app/page.tsx            — 메인 페이지
  - app/layout.tsx          — 공통 레이아웃, 다크 모드 설정
  - components/Hero.tsx     — 이름, 한 줄 소개, 버튼
  - components/Projects.tsx — 프로젝트 카드 그리드
  - components/Contact.tsx  — 이메일 연락처
  - data/projects.ts        — 프로젝트 데이터 (여기서 직접 수정해요)

  ### 작업 순서
  1. Next.js 프로젝트 생성 + Tailwind, shadcn/ui 설치
  2. 다크 모드 + 모바일 반응형 레이아웃 설정
  3. Hero 섹션 구현
  4. 프로젝트 카드 그리드 구현
  5. 연락처 섹션 구현
  6. Vercel 배포 설정
```
</ClaudeBlock>

---

### 5단계 — 계획을 확인하고 승인하거나 수정해요

계획을 보고 마음에 들면 바로 승인하고, 바꾸고 싶은 게 있으면 말하면 돼요.

수정이 필요할 때:

<VibeBlock>
```
> Skills 섹션도 추가해줘. 그리고 프로젝트 카드에 GitHub 링크도 넣어줘.
```
</VibeBlock>

<ClaudeBlock>
```
⏺ 계획 업데이트했어요.
  - Skills 섹션 추가 (components/Skills.tsx)
  - 프로젝트 카드에 GitHub 링크 필드 추가

  이 계획대로 진행할까요?
```
</ClaudeBlock>

바로 승인할 때:

<VibeBlock>
```
> 응, 이대로 진행해줘.
```
</VibeBlock>

---

### 6단계 — Claude가 코드를 작성하기 시작해요

승인하면 Plan 모드가 끝나고 실제 코드 작성이 시작돼요.

<ClaudeBlock>
```
⏺ Plan 모드를 종료하고 구현을 시작할게요.

  ✦ app/layout.tsx 생성 중...
  ✦ app/page.tsx 생성 중...
  ✦ components/Hero.tsx 생성 중...
```
</ClaudeBlock>

<Callout type="info">
  Plan 모드에서 나가기 전에 Claude가 반드시 확인을 요청해요.\
  "응" 또는 "진행해줘"라고 입력하기 전까지는 아무 파일도 바뀌지 않아요.
</Callout>

---

## 로컬 웹서버를 실행해서 확인해요

코드가 완성되면 직접 눈으로 확인해볼 차례예요.\
Claude에게 개발 서버를 실행해달라고 말하면 돼요.

<VibeBlock>
```
> 로컬에서 실행해서 확인해볼게. 개발 서버 켜줘.
```
</VibeBlock>

Claude가 아래처럼 실행해줘요:

<ClaudeBlock>
```
⏺ 개발 서버를 실행할게요.

  $ npm run dev

  ▲ Next.js 15.0.0
  - Local: http://localhost:3000
```
</ClaudeBlock>

브라우저에서 **http://localhost:3000** 을 열어 직접 확인하세요.\
잘못된 부분이 보이면 바로 말해줘도 돼요.

<VibeBlock>
```
> Hero 섹션 글씨가 너무 작아. 좀 더 크게 해줘.

> 프로젝트 카드 배경색이 이상해. 다크 모드에서 텍스트가 안 보여.
```
</VibeBlock>

Claude가 수정하고 개발 서버가 자동으로 반영돼요.\
원하는 모습이 될 때까지 계속 대화하면서 조정하면 돼요.

<Callout type="tip">
  브라우저를 열어두면 코드가 바뀔 때마다 자동으로 새로고침돼요.\
  Claude에게 수정을 요청하면서 실시간으로 결과를 확인할 수 있어요.
</Callout>

---

## 완성됐으면 commit하고 push해요

만족스러운 결과가 나왔다면 변경사항을 저장하고 GitHub에 올릴 차례예요.\
Claude에게 그냥 말하면 돼요.

<VibeBlock>
```
> 좋아, 이대로 커밋하고 푸시해줘.
```
</VibeBlock>

Claude가 변경된 파일을 확인하고 커밋 메시지를 제안해줘요:

<ClaudeBlock>
```
⏺ 변경사항을 확인했어요. 아래 내용으로 커밋할게요.

  feat: 포트폴리오 사이트 초기 구현

  - Hero, Projects, Contact 섹션 추가
  - 다크 모드 및 모바일 반응형 대응
  - 프로젝트 데이터 구조 정의

  커밋하고 push할까요?
```
</ClaudeBlock>

<VibeBlock>
```
> 응, 해줘.
```
</VibeBlock>

<ClaudeBlock>
```
⏺ git add .
  git commit -m "feat: 포트폴리오 사이트 초기 구현"
  git push origin main

  완료됐어요. GitHub에 반영됐어요.
```
</ClaudeBlock>

<Callout type="tip">
  <span className="inline-block text-xs font-semibold tracking-widest uppercase px-2 py-0.5 mb-2 bg-emerald-500/15 text-emerald-600 dark:text-emerald-400 border border-emerald-500/30">Best Practice</span>
  커밋 메시지는 Claude가 자동으로 만들어주지만, 원하는 메시지가 있으면 직접 알려줘도 돼요.\
  **"'초기 포트폴리오 세팅 완료'로 커밋해줘"** 처럼 말하면 그대로 써줘요.
</Callout>

이제 GitHub에 코드가 올라간 상태예요.\
다음 단계에서는 Vercel에 연결해서 실제 URL로 배포해볼 거예요.

---

## 마지막으로 CLAUDE.md를 업데이트해요

구현이 마무리됐다면 CLAUDE.md도 최신 상태로 맞춰주는 게 좋아요.\
코드가 바뀌었으니 Claude가 다음 대화에서 프로젝트를 다시 파악하는 데 걸리는 시간을 줄여줘요.

<VibeBlock>
```
> 지금까지 만든 내용 기준으로 CLAUDE.md 업데이트해줘.
```
</VibeBlock>

Claude가 현재 코드를 분석해서 CLAUDE.md를 새로 써줘요:

<ClaudeBlock>
```
⏺ 현재 프로젝트 구조를 분석하고 CLAUDE.md를 업데이트할게요.

  ✦ CLAUDE.md 업데이트 중...

  아래 내용을 반영했어요:
  - 기술 스택 (Next.js App Router, TypeScript, Tailwind CSS, shadcn/ui)
  - 주요 파일 구조 및 역할
  - 개발 서버 실행 명령어
  - 프로젝트 데이터 수정 위치 (data/projects.ts)
```
</ClaudeBlock>

<Callout type="tip">
  <span className="inline-block text-xs font-semibold tracking-widest uppercase px-2 py-0.5 mb-2 bg-emerald-500/15 text-emerald-600 dark:text-emerald-400 border border-emerald-500/30">Best Practice</span>
  기능을 크게 추가하거나 구조가 바뀔 때마다 CLAUDE.md를 업데이트해두면,\
  다음 대화를 새로 시작해도 Claude가 프로젝트 맥락을 빠르게 파악할 수 있어요.
</Callout>


