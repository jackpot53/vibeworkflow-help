---
title: 스킬 베스트 프랙티스
description: 스킬을 효과적으로 만들고 관리하는 방법을 알아봐요.
---

# 스킬 베스트 프랙티스

## 지침 작성 모범 사례

### 구체적이고 실행 가능하게 작성하기

✅ Good:

<FileBlock filename="SKILL.md — 구체적인 지침 예시">
```markdown
`python scripts/validate.py --input {filename}` 명령어를 실행하여 데이터 형식을 확인하세요.

검증에 실패할 경우, 일반적인 원인은 다음과 같습니다:

- 필수 필드 누락 (CSV에 해당 필드 추가)
- 잘못된 날짜 형식 (YYYY-MM-DD 형식 사용)
```
</FileBlock>

❌ Bad:

<FileBlock filename="SKILL.md — 모호한 지침 예시">
```markdown
진행하기 전에 데이터를 검증하세요.
```
</FileBlock>

---

### 오류 처리 포함

<FileBlock filename="SKILL.md — 오류 처리 섹션">
```markdown
## 일반적인 문제 (Common Issues)

### MCP 연결 실패 (MCP Connection Failed)

"Connection refused" 오류가 발생하는 경우:

1. MCP 서버가 실행 중인지 확인:
   Settings > Extensions에서 확인
2. API 키가 유효한지 확인
3. 재연결 시도:
   Settings > Extensions > [사용 중인 서비스] > Reconnect
```
</FileBlock>

---

### 번들된 리소스를 명확하게 참조하기

<FileBlock filename="SKILL.md — 리소스 참조 예시">
```markdown
쿼리를 작성하기 전에, 다음 내용을 확인하기 위해
`references/api-patterns.md`를 참고하세요:

- Rate limiting(요청 제한) 가이드
- Pagination(페이지네이션) 패턴
- 오류 코드 및 처리 방법
```
</FileBlock>

---

### 점진적 공개 활용

`SKILL.md`에는 핵심 지침에만 집중하세요.\
상세한 문서는 `references/` 폴더로 분리하고, 해당 문서로 연결(link)하세요.

<Callout type="tip">
3단계 구조가 어떻게 작동하는지는 [스킬 만들기](/guides/skills/create-skill#스킬이-로드되는-3단계)를 참고하세요.
</Callout>

---

## Claude가 이미 아는 것은 쓰지 마세요

Claude Code는 코드베이스를 직접 읽고, Claude 자체도 코딩에 대한 판단 기준과 선호를 이미 가지고 있어요. "코드를 명확하게 작성하세요", "에러를 적절히 처리하세요" 같은 지침은 스킬이 없어도 Claude가 알아서 하는 것들이에요.

지식 중심의 스킬이라면, **Claude의 기본 패턴에서 벗어나게 만드는 정보**에만 집중하세요.

Anthropic 엔지니어들이 고객과 반복 개선하며 만든 `frontend-design` 스킬이 좋은 예예요. 이 스킬은 "좋은 UI를 만드세요"가 아니라, Claude가 혼자 두면 반복적으로 선택하는 패턴들 — Inter 폰트, 보라색 그라디언트, 카드 기반 레이아웃 — 을 명시적으로 피하도록 유도해요.

<Callout type="tip">
스킬을 작성하기 전에 스스로에게 물어보세요: **"이 내용이 없으면 Claude가 다르게 행동하는가?"** 대답이 "아니오"라면, 그 문장은 지워도 돼요.
</Callout>

---

## Gotchas (주의사항)

어떤 스킬이든 가장 중요한 섹션은 **Gotchas**예요. Claude가 해당 스킬을 사용할 때 자주 실패하는 지점을 모아두는 곳이에요.

스킬을 처음 만들 때는 비어 있어도 괜찮아요. 실제로 써보면서 Claude가 반복적으로 틀리는 지점이 생기면, 그때그때 추가하세요.

<div className="grid grid-cols-3 gap-4 items-start">
<div>
<p className="text-sm font-semibold text-muted-foreground mb-2">Day 1</p>
<FileBlock filename="SKILL.md">
```markdown
# Billing Lib

내부 빌링 라이브러리 사용법.

전체 API 문서는 라이브러리
README를 참고하세요.
```
</FileBlock>
</div>
<div>
<p className="text-sm font-semibold text-muted-foreground mb-2">Week 2</p>
<FileBlock filename="SKILL.md">
```markdown
# Billing Lib

내부 빌링 라이브러리 사용법.

## Gotchas

- 일할 계산은 올림이 아닌
  내림으로 처리됩니다.
```
</FileBlock>
</div>
<div>
<p className="text-sm font-semibold text-muted-foreground mb-2">Month 3</p>
<FileBlock filename="SKILL.md">
```markdown
# Billing Lib

내부 빌링 라이브러리 사용법.

## Gotchas

- 일할 계산은 내림으로 처리됩니다.
- test-mode에서는
  invoice.finalized 훅 미실행.
- 멱등성 키는 7일이 아닌
  24시간 후 만료됩니다.
- 환불 시 invoice ID가 아닌
  charge ID가 필요합니다.
```
</FileBlock>
</div>
</div>

Claude가 실패할 때마다 한 줄씩 추가하세요.
